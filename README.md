```markdown
# 🔍 Dependency Visualizer

[![Python Version](https://img.shields.io/badge/python-3.6%2B-blue)](https://www.python.org/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

Профессиональный инструмент для анализа и визуализации зависимостей между пакетами. Поддерживает работу с тестовыми репозиториями и реальными NuGet API.

## ✨ Возможности

- **📦 Анализ зависимостей**: Прямые и транзитивные зависимости пакетов
- **🔄 Обратные зависимости**: Поиск пакетов, зависящих от указанного
- **📊 Визуализация**: Генерация графов в формате D2 и ASCII-деревьев
- **🎯 Фильтрация**: Исключение пакетов по подстроке
- **⚡ Гибкая настройка**: Ограничение глубины анализа, различные режимы работы
- **🌐 NuGet Support**: Работа с реальными пакетами из публичных репозиториев

## 🚀 Быстрый старт

### Анализ реального NuGet пакета

```bash
# Прямые зависимости Newtonsoft.Json
python3 config2/stage2.py --package Newtonsoft.Json --repo https://api.nuget.org/v3/index.json --mode real --insecure

# Полный граф зависимостей
python3 config2/stage3.py --package Newtonsoft.Json --repo https://api.nuget.org/v3/index.json --mode real --max-depth 3 --insecure

# Визуализация в D2 и ASCII
python3 config2/stage5.py --repo https://api.nuget.org/v3/index.json --packages Newtonsoft.Json --mode real --ascii --max-depth 2 --insecure
```

### Работа с тестовыми данными

```bash
# Базовый анализ
python3 config2/stage1.py --package A --repo config2/repo.txt --mode test --ascii --max-depth 4 --filter SKIP

# Построение графа
python3 config2/stage3.py --package A --repo config2/repo.txt --max-depth 5 --filter SKIP

# Обратные зависимости
python3 config2/stage4.py --package A --repo config2/repo.txt --max-depth 10

# Визуализация нескольких пакетов
python3 config2/stage5.py --repo config2/repo.txt --packages A B AA --ascii --max-depth 6
```

## 📁 Структура проекта

```
dependency-visualizer/
├── config2/
│   ├── stage1.py    # 🎯 Базовый CLI с валидацией параметров
│   ├── stage2.py    # 📦 Извлечение прямых зависимостей
│   ├── stage3.py    # 🔄 Построение полного графа зависимостей (BFS)
│   ├── stage4.py    # 📊 Поиск обратных зависимостей
│   ├── stage5.py    # 🎨 Визуализация в D2 и ASCII-формате
│   └── repo.txt     # 📝 Тестовый репозиторий
├── examples/        # 🖼️  Примеры графов
└── README.md
```

## 🛠 Установка и настройка

### Требования
- Python 3.6+
- Для работы с реальными NuGet репозиториями: доступ к интернету

### Установка

```bash
# Клонирование репозитория
git clone <your-repo-url>
cd dependency-visualizer

# Проверка Python
python3 --version
```

## 📖 Примеры использования

### Пример 1: Анализ популярного пакета

```bash
# Анализ Newtonsoft.Json
python3 config2/stage2.py --package Newtonsoft.Json --repo https://api.nuget.org/v3/index.json --mode real --insecure

📡 Получение service index: https://api.nuget.org/v3/index.json
🔍 Поиск пакета: https://api.nuget.org/v3/registration5-newtonsoft/12.0.3/newtonsoft.json/index.json
📦 Прямые зависимости пакета 'Newtonsoft.Json' (NuGet):
  └─ Microsoft.NETFramework.ReferenceAssemblies.net461
```

### Пример 2: Визуализация графа

```bash
python3 config2/stage5.py --repo https://api.nuget.org/v3/index.json --packages Newtonsoft.Json --mode real --ascii --max-depth 2 --insecure

🎯 Визуализация для пакетов: Newtonsoft.Json
📊 Режим: real
📏 Макс. глубина: 2

==================================================
📦 Визуализация для пакета: Newtonsoft.Json
✅ D2 файл создан: d2_Newtonsoft_Json.d2

🔍 Предпросмотр D2:
----------------------------------------
// D2 graph generated by Dependency Visualizer

styles: {
  graph: {
    orientation: left
  }
}

Newtonsoft.Json: {
  style: {
    fill: lightblue
    bold: true
  }
}

Newtonsoft.Json -> Microsoft.NETFramework.ReferenceAssemblies.net461
...

🌳 ASCII дерево зависимостей:
----------------------------------------
📦 Newtonsoft.Json
└─ 📦 Microsoft.NETFramework.ReferenceAssemblies.net461
```

## 🎨 Визуализация графов

### Пример графа зависимостей

После генерации D2 файла, вы можете визуализировать его с помощью [D2 Lang](https://d2lang.com/):

```d2
// Пример сгенерированного D2 файла
Newtonsoft.Json -> Microsoft.NETFramework.ReferenceAssemblies.net461
Newtonsoft.Json -> System.Runtime.Serialization.Primitives
Microsoft.NETFramework.ReferenceAssemblies.net461 -> Microsoft.NETFramework.ReferenceAssemblies
```

![Пример графа зависимостей](https://via.placeholder.com/600x400/4A90E2/FFFFFF?text=Graph+Visualization+Example)

### ASCII дерево

```
📦 Newtonsoft.Json
├─ 📦 Microsoft.NETFramework.ReferenceAssemblies.net461
│  └─ 📦 Microsoft.NETFramework.ReferenceAssemblies
└─ 📦 System.Runtime.Serialization.Primitives
   └─ 📦 System.Runtime.Serialization
```

## ⚙️ Параметры командной строки

### Общие параметры:
- `--package, -p`: Имя анализируемого пакета (обязательный)
- `--repo, -r`: Путь к файлу или URL репозитория (обязательный)
- `--mode, -m`: Режим работы (`test` или `real`)
- `--max-depth`: Максимальная глубина анализа (по умолчанию: 5)
- `--filter`: Подстрока для фильтрации пакетов
- `--ascii`: Вывод в формате ASCII-дерева
- `--insecure`: Отключить проверку SSL сертификатов

### Специфические параметры:
- `--packages` (stage5): Список пакетов для визуализации

## 📊 Формат тестового репозитория

Файл `repo.txt` содержит зависимости в формате:

```
ИМЯ_ПАКЕТА: ЗАВИСИМОСТЬ1 ЗАВИСИМОСТЬ2 ...
```

**Пример:**
```
A: B C D
B: E F
C: G H
# Комментарии поддерживаются
P: A   # Циклические зависимости обрабатываются
```

## 🔧 Поддерживаемые пакеты

### NuGet пакеты:
- ✅ Newtonsoft.Json
- ✅ Microsoft.Extensions.DependencyInjection  
- ✅ Serilog
- ✅ AutoMapper
- ✅ NUnit
- ✅ И многие другие...

## 🐛 Решение проблем

### SSL ошибки при работе с NuGet

```bash
# Используйте флаг --insecure для тестирования
python3 config2/stage2.py --package Newtonsoft.Json --repo https://api.nuget.org/v3/index.json --mode real --insecure
```

### Пакет не найден

Убедитесь, что пакет существует в репозитории:

```bash
# Для тестового режима
python3 config2/stage2.py --package A --repo config2/repo.txt --mode test

# Для реального режима проверьте имя пакета
python3 config2/stage2.py --package Newtonsoft.Json --repo https://api.nuget.org/v3/index.json --mode real --insecure
```

### Ограничение глубины анализа

При больших графах используйте `--max-depth`:

```bash
python3 config2/stage3.py --package Newtonsoft.Json --repo https://api.nuget.org/v3/index.json --mode real --max-depth 2 --insecure
```
